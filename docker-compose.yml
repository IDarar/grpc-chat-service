version: "3.8"
    
services:
  mysql:
    image: mysql:latest
    volumes:
      - db_data:/var/lib/mysql
    #restart: always
    container_name: mysql  
    env_file:
        - .env  
    ports: 
        - 3306:3306
volumes:
  db_data:    
    driver: local

